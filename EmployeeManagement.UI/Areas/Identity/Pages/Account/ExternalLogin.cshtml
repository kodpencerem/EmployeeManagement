@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Kayıt Ol";
}

<h1>@ViewData["Title"]</h1>
<h4>@Model.LoginProvider ile hesabınızı ilişkilendirin.</h4>
<hr />

<p class="text-info">
    <strong>@Model.LoginProvider</strong> ile başarılı bir şekilde doğruladınız.
    Lütfen aşağıya bu site için bir e-posta adresi girin ve oturum açmayı tamamlamak için Kaydol düğmesini tıklayın.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"><strong>E-Posta Adresiniz:</strong></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Kayıt Ol</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
